# チャットプロキシサーバー要件定義書

## 1. プロジェクト概要

### 目的

既存のチャットサーバー（HTTPでJSONリクエストをPOST、Server-Sent Events形式でJSONレスポンスを返却）をOpenAI API `/v1/chat/completions` 互換のインターフェースでラップするプロキシサーバーを構築する。

### 利用者

- 1人での利用を想定
- 多数の同時接続は不要

### 設計方針

- 可能な限り依存パッケージを最小化
- 段階的開発によるリスク低減

## 2. 技術仕様

### 開発言語

- Python 3.11+

### 段階的開発戦略

#### Phase 1: Flask版（プロトタイプ・動作確認）

**目的**: 基本機能の実装と動作確認

**技術スタック**:

- Flask 3.0.0
- requests 2.31.0

**メリット**:

- デバッグが容易
- 実装が簡単
- 豊富な情報とコミュニティサポート

#### Phase 2: FastAPI版（パフォーマンス最適化）

**目的**: 必要に応じたパフォーマンス向上

**技術スタック**:

- FastAPI 0.104.1
- uvicorn 0.24.0
- httpx 0.25.2

**移行条件**:

- Flask版が安定動作後
- パフォーマンス要件が発生した場合

## 3. アーキテクチャ設計

### ディレクトリ構成

```
chat_proxy/
├── app_flask.py           # Flask版メインアプリケーション
├── app_fastapi.py         # FastAPI版メインアプリケーション（Phase 2）
├── core/                  # 共通ビジネスロジック
│   ├── __init__.py
│   ├── converter.py       # データ変換ロジック
│   ├── client.py          # 既存サーバー通信クライアント
│   └── models.py          # データ構造定義
├── config.py              # 設定管理
├── requirements/
│   ├── flask.txt          # Flask版依存関係
│   └── fastapi.txt        # FastAPI版依存関係（Phase 2）
└── README.md
```

### 設計原則

- **関心の分離**: フレームワーク固有のコードと共通ロジックを分離
- **再利用性**: Phase 2移行時にcore/モジュールをそのまま利用可能
- **保守性**: 各モジュールの責務を明確化

## 4. 機能要件

### 4.1 API仕様

OpenAI API `/v1/chat/completions` エンドポイントとの完全互換性

#### サポートパラメータ

**必須**:

- `model`: モデル名
- `messages`: メッセージ配列（role, content）

**オプション**:

- `temperature`: 温度パラメータ（デフォルト: 1.0）
- `max_tokens`: 最大トークン数
- `stream`: ストリーミング有効/無効（デフォルト: false）
- `stop`: 停止文字列

#### レスポンス形式

- ストリーミング: Server-Sent Events形式
- 非ストリーミング: JSON形式
- OpenAI APIと同一の構造

### 4.2 データ変換機能

- **リクエスト変換**: OpenAI形式 → 既存サーバー形式
- **レスポンス変換**: 既存サーバー形式 → OpenAI形式
- **ストリーミング変換**: SSE → OpenAI chunk形式

### 4.3 通信機能

- 既存サーバーとのHTTP通信
- ストリーミングレスポンスの適切な処理
- タイムアウト処理（デフォルト: 30秒）

## 5. 非機能要件

### 5.1 パフォーマンス

- **Phase 1**: 1人利用に十分な性能
- **Phase 2**: 必要に応じて非同期処理による最適化

### 5.2 信頼性

- 適切なエラーハンドリング
- 既存サーバー障害時の適切なエラーレスポンス
- 接続タイムアウトの処理

### 5.3 保守性

- 詳細なログ出力
- ヘルスチェック機能（`/health`エンドポイント）
- デバッグモード対応

### 5.4 セキュリティ

- 基本的なエラーハンドリング
- 適切なHTTPヘッダー設定

## 6. ライセンス考慮事項

### 使用ライブラリのライセンス

**Phase 1**:

- Flask: BSD-3-Clause
- requests: Apache-2.0

**Phase 2**:

- FastAPI: MIT License
- uvicorn: BSD-3-Clause
- httpx: BSD-3-Clause

**結論**: 全て商用利用可能、制約なし

## 7. 開発・運用要件

### 7.1 デバッグ容易性

- **Phase 1**: Flaskの詳細なエラー表示機能
- インタラクティブデバッガー対応
- ステップ実行によるデバッグ支援

### 7.2 設定管理

- 環境変数による設定
- 既存サーバーURL設定
- タイムアウト設定

### 7.3 起動方法

```bash
# Phase 1: Flask版
python app_flask.py

# Phase 2: FastAPI版
uvicorn app_fastapi:app --host 0.0.0.0 --port 8000
```

## 8. 実装優先順位

### 優先度1（Phase 1必須機能）

1. 基本的なAPI互換性実装
1. 非ストリーミングチャット機能
1. ストリーミングチャット機能
1. 基本的なエラーハンドリング

### 優先度2（Phase 1追加機能）

1. ヘルスチェック機能
1. 詳細ログ出力
1. 設定の外部化

### 優先度3（Phase 2検討事項）

1. パフォーマンス測定
1. FastAPI版への移行判断
1. 追加機能の検討

## 9. テスト戦略

### 単体テスト

- 各core/モジュールの単体テスト
- データ変換機能のテスト

### 統合テスト

- 既存サーバーとの通信テスト
- OpenAI API互換性テスト

### 動作確認

- 実際のチャットクライアントでの動作確認
- ストリーミング・非ストリーミング両方の確認

## 10. 想定リスクと対策

### リスク1: 既存サーバーとの互換性問題

**対策**: 詳細なログ出力とエラーハンドリングで問題箇所を特定しやすくする

### リスク2: ストリーミング処理の複雑性

**対策**: Flask版で十分にテストしてからFastAPI版に移行

### リスク3: パフォーマンス不足

**対策**: Phase 2でFastAPI版への移行で対応

-----

**文書バージョン**: 1.0  
**作成日**: 2025年6月19日  
**最終更新**: 2025年6月19日